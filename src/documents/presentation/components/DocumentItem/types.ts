export type DocumentItemSlots = {
  name: HTMLElement;
  version: HTMLElement;
  relativeCreatedAt: HTMLElement;
  contributors: HTMLElement;
  attachments: HTMLElement;
};

export type LayoutType = "grid" | "list";
